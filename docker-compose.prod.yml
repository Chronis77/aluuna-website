version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    networks:
      - aluuna-prod-network
    restart: unless-stopped

networks:
  aluuna-prod-network:
    driver: bridge
